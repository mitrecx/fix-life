import { TodosList } from "@/components/TodosList";

export default function TodosPage() {
  return (
    <div className="relative -mx-3 -my-3 sm:-mx-4 sm:-my-4 h-full min-h-0 overflow-hidden">
      <TodosList />
    </div>
  );
}
